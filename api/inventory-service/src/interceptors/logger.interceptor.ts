// Re-exported from @bidbay/logger — the LoggerModule registers this automatically
// as APP_INTERCEPTOR when imported via LoggerModule.forService().
export { LoggerInterceptor } from '@bidbay/logger';
